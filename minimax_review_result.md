**BUG — broken right now**

| # | file | section | issue | suggested fix |
|---|------|---------|-------|---------------|
| 1 | mr-do-asound-cfgmap.yml | `metadata` | ConfigMap has no `namespace` field (the sibling `mr-do-snapserver-cfgmap` does). With plain `kubectl apply -f` it lands in `default`, and the Deployment's `volumes[].configMap.name: mr-do-asound-cfgmap` lookup fails at pod start. | Add `namespace: mr-do-player` to metadata. |
| 2 | mr-do-asound-cfgmap.yml, mr-do-snapserver-cfgmap.yml | `metadata.creationTimestamp` + commented `resourceVersion`/`uid` | `creationTimestamp: "2025-01-07T22:51:48Z"` is hard-coded (kubectl-export artifact) and the commented `resourceVersion`/`uid` blocks are leftover. ArgoCD will diff these every reconcile; some admission paths reject a user-set creationTimestamp. | Delete `creationTimestamp`, `resourceVersion`, `uid` from the committed manifests. |
| 3 | mr-do-player-deployment.yml + mr-do-asound-cfgmap.yml + mr-do-snapserver-cfgmap.yml | `containers[0].volumeMounts` (mr-do-upnp) ↔ `pcm.writeFile.file` ↔ `[stream] stream = pipe:///tmp/music/upnpfifo` | The shared FIFO `/tmp/music/upnpfifo` is opened by ALSA's `file` plugin (writer, `gmediarender` starts first) and by Snapserver's `pipe://` source (reader, starts second). Nothing in the manifests creates it: ALSA's `file` plugin auto-creates a *regular* file when the path is missing, after which Snapserver fails to open it as a FIFO and the stream source errors out. | Add an `initContainer` (e.g. `image: busybox:1.36`, `command: ["sh","-c","mkfifo -m 0666 /tmp/music/upnpfifo"]`) sharing the `mr-do-snapserver-data` emptyDir, so the FIFO exists before either container opens it. |
| 4 | mr-do-player-pv.yml | `spec.persistentVolumeReclaimPolicy` | The only reclaim policy is commented out (`#  persistentVolumeReclaimPolicy: Recycle`). Default for a newly created PV is `Delete`, which removes the PV object on PVC release — for a manually bound static NFS volume this orphans the binding and requires hand-recreating the PV before any future PVC can use it. (Note: `Recycle` itself is deprecated since 1.20.) | Set `persistentVolumeReclaimPolicy: Retain`. |

**RELIABILITY**

| # | file | section | issue | suggested fix |
|---|------|---------|-------|---------------|
| 1 | mr-do-player-deployment.yml | `containers[0].image` | `image: riemerk/mr-do-upnp:latest` — floating `:latest` with default `imagePullPolicy: Always`; no digest pin, non-reproducible, every rollout re-pulls. | Pin to a tag (e.g. `:0.5`) or `@sha256:…` digest and set `imagePullPolicy: IfNotPresent`. |
| 2 | mr-do-player-deployment.yml | `spec.revisionHistoryLimit` | Not set; etcd keeps the default 10 ReplicaSets forever. | Set `revisionHistoryLimit: 3`. |
| 3 | (missing file) | n/a | No `PodDisruptionBudget` for the single-replica hostNetwork deployment. A voluntary node drain (upgrade, autoscaler) takes down the only audio endpoint with no safety net. | Add `kind: PodDisruptionBudget` with `maxUnavailable: 0` matched by `app: mr-do-player`. |
| 4 | mr-do-player-deployment.yml | `containers[0]` (mr-do-upnp) | No `livenessProbe`/`readinessProbe` on the gmediarender side. Only `mr-do-snapserver` is probed, so a wedged gmediarender still keeps the Service endpoint green. | Add `livenessProbe.tcpSocket.port: upnp-http` and `readinessProbe.tcpSocket.port: upnp-http`. |
| 5 | mr-do-player-pv.yml | `spec.mountOptions` | The `hard` and `nfsvers=4.1` mount options are commented out. Without `hard`, transient NFS server blips can wedge the kubelet; without `nfsvers=4.1` you fall back to NFSv3 with weaker locking semantics. | Uncomment and set `mountOptions: ["hard", "nfsvers=4.1", "timeo=600", "retrans=3", "intr"]`. |
| 6 | mr-do-player-deployment.yml | `spec.template.spec.terminationGracePeriodSeconds` | Not set; default 30 s. SIGKILLing snapserver mid-stream can leave the FIFO in a half-open state and confuse the next pod start. | Set `terminationGracePeriodSeconds: 60` (or higher) and ensure containers handle SIGTERM (drain clients, close FIFO). |
| 7 | mr-do-player-deployment.yml | `spec.strategy` | Default `RollingUpdate` with `replicas: 1` is benign today, but if you ever scale, the two containers bind ports 49494/1900/1780/1704/1705/42661/5353 directly on the host network, so a rolling update will collide with itself. | Set `strategy: { type: Recreate }` (required for hostNetwork pods that share node ports). |
| 8 | mr-do-player-service.yml | `spec.externalTrafficPolicy` | `externalTrafficPolicy: Local` + a single hostNetwork pod means: any time the pod is rescheduled, in `CrashLoopBackOff`, or in the gap between node drain and ready, the LB has zero healthy endpoints and 192.168.0.20 silently blackholes. | Either accept and document explicitly, or switch to `externalTrafficPolicy: Cluster` (or use a headless Service + DNS-only for the multicast ports). |
| 9 | mr-do-snapserver-cfgmap.yml | `data.snapserver.conf` → `[stream] stream = pipe:///tmp/music/upnpfifo` | Even if the FIFO exists, snapserver opens it at startup before gmediarender has anything to write; if the writer side never comes up the source remains in a stuck "no peer" state with no observable failure. | Combine with the BUG-#3 init container and add a snapserver-side readiness condition that fails the `mr-do-snapserver` container until the pipe has at least one writer (e.g. `livenessProbe.exec` that `stat -c %F`s the FIFO). |

**SECURITY**

| # | file | section | issue | suggested fix |
|---|------|---------|-------|---------------|
| 1 | mr-do-player-deployment.yml | `containers[].securityContext` | No `securityContext` on either container — no `runAsUser`, no `runAsNonRoot`, no `readOnlyRootFilesystem`, no `allowPrivilegeEscalation: false`. Containers run as whatever the image's `USER` is (root for snapserver, since `/root/.config/snapserver` is hard-coded). | Add a `securityContext` per container: `runAsNonRoot: true`, `runAsUser: 1000`, `runAsGroup: 1000`, `allowPrivilegeEscalation: false`, `readOnlyRootFilesystem: true` (with writable `emptyDir` mounts where required), `capabilities.drop: ["ALL"]`. Note: the PVC mount path `/root/.config/snapserver` will need to move (e.g. `/var/lib/snapserver`) to honour `runAsNonRoot: true`. |
| 2 | mr-do-player-deployment.yml | `containers[].securityContext.capabilities` | Even with hostNetwork the process does not need any Linux capabilities — none are dropped. | Set `capabilities.drop: ["ALL"]` (and add nothing, since librespot/snapserver don't need caps). |
| 3 | mr-do-player-deployment.yml | `containers[].securityContext.seccompProfile` | No `seccompProfile` set; pod inherits whatever the runtime default is. | Set `seccompProfile: { type: RuntimeDefault }` explicitly. |
| 4 | mr-do-player-deployment.yml | `containers[0].volumeMounts` / `containers[1].volumeMounts` | `readOnlyRootFilesystem` is not set, so the container rootfs is writable; combined with `hostNetwork: true` and no `runAsNonRoot`, an RCE in either image gets a writable host-reachable filesystem. | Set `readOnlyRootFilesystem: true` and mount writable `emptyDir`s at `/tmp`, `/var/run`, `/root/.config` etc. |
| 5 | mr-do-player-deployment.yml | `spec.template.spec.automountServiceAccountToken` | Not set; pod mounts the default `argocd`-namespace SA token into `/var/run/secrets/kubernetes.io/serviceaccount`, which neither container needs. | Set `automountServiceAccountToken: false`. |
| 6 | mr-do-player-deployment.yml | `spec.template.spec.containers[].imagePullPolicy` / no `imagePullSecrets` | No `imagePullPolicy` override and no `imagePullSecrets`. For `riemerk/mr-do-*` from Docker Hub this works anonymously, but there is no provenance verification (no cosign/sigstore, no `:sha256-…` digest pin), so a hijacked upstream image runs immediately. | Pin images by digest and (optionally) add a Kyverno/Gatekeeper policy that rejects floating tags. |

**MAINTAINABILITY**

| # | file | section | issue | suggested fix |
|---|------|---------|-------|---------------|
| 1 | all manifests | `metadata.labels` | No `app.kubernetes.io/{name,instance,component,part-of,managed-by}` labels on Deployment, Service, PVC, PV, ConfigMaps, or the ArgoCD Application. Standard tooling (kustomize, ArgoCD, kubectl-tree, dashboards) can't group by app. | Add the standard label set (`app.kubernetes.io/name`, `app.kubernetes.io/instance`, `app.kubernetes.io/managed-by=kustomize-controller` or `argocd`, plus `app.kubernetes.io/component` if useful). |
| 2 | mr-do-player-deployment.yml | `spec.selector.matchLabels`, `spec.template.metadata.labels`, Service selector | Uses non-standard `app: mr-do-player` everywhere. The same convention should use `app.kubernetes.io/name`. | Replace `app:` with `app.kubernetes.io/name: mr-do-player` (keep `app` as a transitional alias if anything else selects on it). |
| 3 | mr-do-player-pv.yml | `metadata.labels`, `spec` selector | Uses non-standard label `usage: mr-do-player-pv-data` for PVC binding. Works, but couples PVC↔PV through an ad-hoc key. | Use `app.kubernetes.io/instance: mr-do-player` + a `purpose: data` label, or split binding via `storageClassName`. |
| 4 | mr-do-asound-cfgmap.yml, mr-do-snapserver-cfgmap.yml, mr-do-player-pv.yml | (multiple commented lines) | Leftover `kubectl get -o yaml --export` artifacts: commented `resourceVersion`, `uid`, `mountOptions`, `persistentVolumeReclaimPolicy`, `app.kubernetes.io/instance`. They confuse diffs and bury real intent. | Delete the commented lines; promote the kept values (`Retain`, `hard/nfsvers=4.1`) into the live spec. |
| 5 | mr-do-player-app.yaml | `spec.source.targetRevision` | `targetRevision: main` is a floating branch reference — every ArgoCD sync pulls the latest commit, making rollbacks and audits impossible. | Pin to a tag (`vYYYY.MM.DD-sha`) or a commit SHA. |
| 6 | mr-do-player-app.yaml | `spec.syncPolicy.syncOptions` | Only `CreateNamespace=true`. Missing `ServerSideApply=true` (avoid field-management fights), `ApplyOutOfSyncOnly=true` (smaller patches), `PruneLast=true`, `PrunePropagationPolicy=foreground`. | Add `ServerSideApply=true`, `ApplyOutOfSyncOnly=true`, `PruneLast=true`, `PrunePropagationPolicy=foreground`. |
| 7 | mr-do-player-app.yaml | `spec.syncPolicy.ignoreDifferences` / `retry` | No `ignoreDifferences` for the known metadata drift (server-set `resourceVersion`/`uid`, controller-set labels/annotations) — App will be perpetually `OutOfSync`. No `retry` block — transient sync failures don't auto-recover. | Add `ignoreDifferences` for `mp.metadata.labels` (ArgoCD) and `jp.metadata.resourceVersion`; add `retry: { limit: 5, backoff: { duration: 10s, factor: 2, maxDuration: 3m } }`. |
| 8 | mr-do-player-deployment.yml | `spec.template.spec.volumes[].name` | Volume names are inconsistent with the ConfigMaps they wrap: `mr-do-asound-cfgm` ↔ `mr-do-asound-cfgmap`, `mr-do-snapserver-cfgm` ↔ `mr-do-snapserver-cfgmap`. Cosmetic, but every reviewer wastes a cycle verifying they really match. | Rename volumes to `mr-do-asound-cfgmap` / `mr-do-snapserver-cfgmap` (drop the trailing `m`). |
| 9 | mr-do-player-service.yml | `spec.ports` (1900/UDP, 5353/UDP) | SSDP (1900/UDP, 239.255.255.250) and mDNS (5353/UDP, 224.0.0.251) are multicast; a Kubernetes Service cannot DNAT multicast. These two port entries are misleading and suggest a forwarding path that doesn't exist. Real multicast relies entirely on `hostNetwork`. | Move the comment block from the Deployment up here (currently the Deployment explains multicast — the Service contradicts it); either drop the two ports from the Service or annotate them as `service.kubernetes.io/no-mesh-endpoints`/document them as no-op for multicast. |